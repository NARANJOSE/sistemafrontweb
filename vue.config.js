module.exports = {
  transpileDependencies: [], // 👈 Aquí va un array (vacío si no tienes nada específico que transpilar)
  lintOnSave: false,

  pluginOptions: {
    vuetify: {
      // vuetify-loader config
    },
  },
};
